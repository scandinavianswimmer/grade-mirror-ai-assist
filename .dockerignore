# Build context = repo root for deploy/cloud-run/Dockerfile (it copies supabase/functions).
# Keep the context lean: exclude the frontend toolchain, deps, and local state. Do NOT exclude
# supabase/functions or deploy/ — those are what the image needs.
node_modules
.git
dist
public
src
eval
scripts
.planning
.agents
*.log
.env
.env.*
.DS_Store
bun.lockb
package-lock.json
worker/node_modules
